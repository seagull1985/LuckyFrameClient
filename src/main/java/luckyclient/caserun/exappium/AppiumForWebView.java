package luckyclient.caserun.exappium;

public class AppiumForWebView {
/*	
	public static void FindWebView(){
		Set<String> contextNames = appium.getContextHandles();
		System.out.println(contextNames.size());
		for (String contextName : contextNames) {
		System.out.println(contextName);
		if (contextName.contains("WEBVIEW")){
			appium.context(contextName);
			if (appium.findElementsByName("银盛通主要功能").size() != 0) {
				JavaBaseTest.LogUtil.APP.info("检查功能介绍成功");
				appium.sendKeyEvent(AndroidKeyCode.BACK);
				result = "检查功能介绍成功";
			} else {
				JavaBaseTest.LogUtil.ERROR.error("检查功能介绍失败");
				APPScreenShot(appium, PROJECTWORK_PATH);
				appium.sendKeyEvent(AndroidKeyCode.BACK);
			}
			break;
		}else{
		System.out.println("no WEBVIEW"); 
		}
		}
	}
*/
}
