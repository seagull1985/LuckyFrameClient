说明:
1：此TestDriven文件夹是默认存放测试驱动桩的路径
2：您只需要把测试驱动桩打成JAR以及它所依赖的JAR全部放到此路径下即可
3：在调度任务配置界面默认就是此路径
4：如果您有多个项目需要在同一台客户端机器运行，为了避免包冲突，请另外再按项目新建文件夹，在调度任务配置界面修改对应的路径即可
5：调度任务的路径填写只需要填写此客户端项目路径后的路径，如您现在客户端TestDriven的路径是 C:\LuckyFrameClient\TestDriven，那么您只需要配置/TestDriven（注意，这地方是反斜杠）。